

## Traduzir "Workflow" para cada idioma

### Alteração

**Arquivo: `src/lib/i18n/translations.ts`**

Atualizar a chave `nav.projects` em cada idioma:

- **EN** (linha ~17): `"Workflow"` → `"Workflow"` (mantém, já é termo universal em inglês)
- **PT** (linha ~1799): `"Workflow"` → `"Fluxo de Trabalho"`
- **ES** (linha ~3529): `"Workflow"` → `"Flujo de Trabajo"`

Apenas 1 arquivo, 2 linhas alteradas.

